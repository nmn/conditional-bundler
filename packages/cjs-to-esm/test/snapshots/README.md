# CJS Package Corpus Snapshots

These files are generated by the `@bundler/cjs-to-esm` Babel plugin from every
shipped `.js` and `.cjs` file in the pinned package corpus.

Run `pnpm --filter @bundler/cjs-to-esm snapshots:update` after an intentional
transform or corpus dependency change, then review the resulting file diffs.
