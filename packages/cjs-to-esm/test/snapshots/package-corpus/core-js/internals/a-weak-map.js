import { has as _has } from "../internals/weak-map-helpers";
var has = _has;

// Perform ? RequireInternalSlot(M, [[WeakMapData]])
const _cjs_default = function (it) {
  has(it);
  return it;
};
export default _cjs_default;
