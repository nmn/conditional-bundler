# CLI

```bash
bundler build --entry src/index.js --out dist/bundle.js
```

## Commands

- `build`: Build bundles for configured entries
- `clean-cache`: Clear the disk cache
- `analyze`: Emit manifest and graph details
