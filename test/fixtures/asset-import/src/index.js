import logoUrl from "./logo.svg";
// eslint-disable-next-line no-unused-vars
import unusedUrl from "./unused.svg";

export const logo = logoUrl;
export const freshLogo = new URL("./logo.svg", import.meta.url);
